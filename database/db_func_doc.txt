DOCUMENTATION for "db_func.py" (database functionality)
    -Edel Barcenas

Database Info: 
    url : mysql://be3c5020bf5627:3bfc3cfa@us-cdbr-east-04.cleardb.com/heroku_f7729a769a3bca5?reconnect=true
    username : be3c5020bf5627
    password : 3bfc3cfa
    hostname : us-cdbr-east-04.cleardb.com
    database : heroku_f7729a769a3bca5
    
    
Public Functions (for use when db_func is imported):
    addUser(string fname, string lname, string username, string password, string email, int isAdmin ):
        -task : adds user with specified attributes to the database
        -all strings must be < 20 chars
        -isAdmin must be 1 | 0
        
    GETTER METHODS ( all gets use __getUserInfo() ) :
        
        getFname(string username):
        
        getLname(string username):
        
        getPassword(string username):
        
        getEmail(string username):
        
        getAdminStatus(string username): 
            -returns 1 if username is an Admin | 0 if not
        
        getBlockStatus(string username):
            -returns 1 if username is blocked | 0 if not
    
    SETTER METHODS ( all sets use __runQuery() ) : 
        
        setFname(string username, string new_fname):
        
        setLname(string username, string new_lname):
        
        setUsername(string old_username, string new_username):
            -all strings must be < 20 chars
        
        setPassword(string username, string new_password):
        
        setEmail(string username, string new_email):
        
        setAdminStatus(string username, int status):
            -status must be 0 | 1
        
        setBlockStatus(string username, int status):
            -status must be 0 | 1
        
        
Private Variables (not for import usage):
    __drop_table_commands : 
        -a dictionary with format [ table name : SQL command to drop/delete table ]
        
    __init_table_commands : 
        -a dictionary with format [ table name : SQL command to initialize/create table ]
    
Private Functions (not for import usage): 

    __connect(): this file does work as intended currently, variables are useless out of scope
        -task : establishes connection to database and initializes cursor
        
    __disconnect(): idk if this works or not, it's good to close stuff but idk if this does it. It works with or without it.
        -task : closes connection to database and pointer

    __initDB():
        -task : resets the database. Wipes all table data and re-initializes all tables
        -uses __setTableCommands()
        -uses commands present in __drop_table_commands & __init_table_commands
        
    __setTableCommands():
        -task : initializes __drop_table_commands & __init_table_commands
        -side comment : I'd prefer to have the commands in a seperate .sql file but the cursor.execute() was giving me problems

    __runQuery(string query , tuple data):
        -task : connects to database to then runs SQL query

    __getUserInfo(string username):
        -task : returns a tuple of all user information
            tuple format: ( first_name, last_name, username, password, email, admin_status, block_status )
    